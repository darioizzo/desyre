# Mandatory public dependency on Boost.
find_package(Boost 1.60 REQUIRED serialization)

# Get current dir.
get_filename_component(_dsyre_CONFIG_SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(${_dsyre_CONFIG_SELF_DIR}/dsyre_export.cmake)

# Clean up.
unset(_dsyre_CONFIG_SELF_DIR)
